.nav.nav-tabs(id="profile-nav")
	.nav-item
		a.nav-link(href='/profile/'+userSession.id) Overview
	.nav-item
		a.nav-link(aria-current="page" href='/profile/'+userSession.id+'/alg-collection') Algorithm collections
	.nav-item
		a.nav-link(href='/profile/'+userSession.id+'/my-cubes') Cubes